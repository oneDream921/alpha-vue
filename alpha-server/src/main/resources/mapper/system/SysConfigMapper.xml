<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.onedream921.alphavue.modules.system.mapper.SysConfigMapper">

    <sql id="ConfigColumns">
        id, config_key, config_value, description, deleted, created_at, updated_at
    </sql>

    <select id="selectPageActive" resultType="io.github.onedream921.alphavue.modules.system.entity.SysConfig">
        SELECT <include refid="ConfigColumns"/>
        FROM sys_config
        WHERE deleted = 0
        ORDER BY id DESC
    </select>

    <select id="selectActiveById" resultType="io.github.onedream921.alphavue.modules.system.entity.SysConfig">
        SELECT <include refid="ConfigColumns"/>
        FROM sys_config
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectActiveByConfigKey" resultType="io.github.onedream921.alphavue.modules.system.entity.SysConfig">
        SELECT <include refid="ConfigColumns"/>
        FROM sys_config
        WHERE config_key = #{configKey} AND deleted = 0
    </select>

    <insert id="insertConfig" useGeneratedKeys="true" keyProperty="config.id">
        INSERT INTO sys_config (config_key, config_value, description, deleted, created_at, updated_at)
        VALUES (#{config.configKey}, #{config.configValue}, #{config.description}, 0, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateConfig">
        UPDATE sys_config
        SET config_key = #{config.configKey},
            config_value = #{config.configValue},
            description = #{config.description},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{config.id} AND deleted = 0
    </update>

    <update id="softDeleteById">
        UPDATE sys_config
        SET deleted = 1, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>
</mapper>
