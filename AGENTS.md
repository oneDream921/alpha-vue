<!-- 由 Agent Framework Kit 生成。请勿直接编辑；请更新 .project-agent 内容后运行 agent sync。 -->
# Codex 工作区指南

## 框架通用规则

### engineering

# 工程约定

变更范围限定在请求目标内。优先清晰模块边界、显式输入输出，并遵循项目既有约定。避免无关重构；交付前验证行为。

## 冲突优先级

发生约定冲突时，按以下顺序决策（前者优先）：

1. 同文件 / 同模块**邻居真实代码**与既有调用点  
2. **项目** Rule / Skill / 记忆 / 规格文档  
3. 本框架 Core Rule  
4. 通用语言 / 框架习惯  

扩展存量代码时先对齐邻居再引入新风格；不要因为「更通用」覆盖项目已形成的强约定。

## 按需加载

相关任务先读记忆索引或用户点名的规范，再按需下钻；**不要**一次把整仓 AI 入口、全套规格或全部 Skill 塞进上下文。

动手写业务代码前：先按 Git 约定确认不在生产分支（或其它受保护分支）上盲改（未迁出则先停下提示迁出）；再简要说明设计思路、改动范围、影响点；模糊处先问清再改。不要求必须先做需求探索。

## 内容职责

- **Rule**：只承载硬约束、禁止项、确认点，以及何时应使用某个 Skill；不要重复完整操作步骤。
- **Skill**：承载可复用的具体工作流，说明何时使用、前置条件、执行步骤、完成条件和验证 / 汇报方式。
- **Hook**：只承载可确定执行的安全拦截或检查；不要把规划、反思、重试或记忆判断等模型决策放入 Hook。

### git

# Git 约定

保留工作区中与当前任务无关的改动。提交前检查 diff；每个提交应是一个完整连贯的单元。

## 专题分支（开发前）

**推荐顺序：先有短名 → 先迁出专题分支 → 再落盘需求 / SQL 或改业务代码。** 禁止以「在生产分支上开发，提交时再迁出」为常规做法——需求与 DDL 按当前分支名归档，落在生产分支名下事后无法自动纠正。

不依赖需求探索：一句话任务即可收成 `<slug>` 并建议迁出。

### 如何识别「受保护分支」

读取项目 Rule「分支角色」：

1. **生产分支**（主触发）：当前分支等于该字段值时，视为受保护。  
2. **额外禁止直改**（可选）：若项目另声明禁止直接开发的分支（或把集成分支也标为勿直改），一并视为受保护。  
3. **未配置时**：先提示补全「生产分支」等字段；临时把 `main` / `master` 当作受保护分支兜底。

### 迁出提示（受保护分支上要改码 / 落盘时）

在目标仓执行 `git rev-parse --abbrev-ref HEAD`。若当前属于受保护分支，且即将改业务代码、持久化映射、形成需求文档或写入 `scripts/sql/`，则**本轮只做迁出引导，禁止写业务文件**：

1. 主题够清晰则直接建议分支；否则先问 **1～2 个大致需求**，再给分支建议。  
2. 按项目「功能分支 / 修复分支」模板给**一条**推荐名与完整命令（未配置则用 `feat/<slug>` / `fix/<slug>`），**不代执行**。  
3. 末尾必问是否迁出成功；成功后回复「继续」再往下做。  
4. 用户未确认前不得实现或落盘；确认后可再核对当前分支。

「直接改 / 帮忙实现」不等于授权在生产分支动手；仅当本轮明确「就在该生产分支上改」才例外。仅探索 / 问答 / 只读排查不必迁出。

**缺省配置**：关键字段未填时，先建议补全项目 Rule，再给临时建议，收尾再提醒补配置。  
**Agent 编辑项目 Rule**：只增量补全，不删已填条目；删改须用户明确指令。  
**`init` / `sync`**：用户资产（含本文件）存在即保留，不覆盖——与 Agent 编辑约束分开。

### 补救（已在受保护分支误改 / 准备提交）

- 有未提交改动仍在受保护分支：不要在此 commit / 归档；建议 `git switch -c <专题分支>`，问是否成功，「继续」后再做。  
- 进入提交语境且仍在受保护分支：先提示风险与迁出命令，确认后再给分批提交建议。  
- 误提交已进受保护分支：不代 reset/rebase，只交恢复命令给用户。

例外：用户明确就在当前受保护分支做；纯文档 / 注释 / 格式且不写需求与 SQL；或用户已指明发布 / hotfix 目标分支。

**能力边界**：迁出命令由 Cursor / Codex Hook 硬拦；「禁止在受保护分支改代码」目前是 Rule 软约束。

## 合入（软约束）

分支名与合入偏好以项目 Rule 为准。Agent **不代执行** merge / rebase / pull（改工作区）/ 向生产·集成·预发·测试·release 等的 push，以及未经本轮明确要求的代开 PR。

用户问如何合入时：按项目「集成分支 / 合入偏好」给出**本地合入**与 **PR/MR** 两条可选步骤（有偏好则偏向该条，仍保留备选），由用户自执行。合入预发 / 测试 / release 同理。

### 多 Git 仓

各仓拓扑可不同。先按多仓路由锁定目标仓，再读该仓分支角色；命令带仓路径语境。跨仓需求逐仓建议迁出与合入，禁止一次 merge / 一次 PR。需求归档取「主业务仓」分支名，可与改码仓不同，两处都要问清。

## 行为红线

- **本地提交**：仅当用户在本轮明确说出「确认提交」时，才可执行 `git add` / `git commit`。「提交」「commit」「帮我提交」等均不执行；只输出分批方案与**可复制的完整提交命令**（见下方「提交命令必须带 message」）。
- **远程与危险操作**：`push` / `pull` / `merge` / `rebase` / `reset` / `checkout` / `switch` / `stash` 等一律不代执行（即使用户口头要求）；把完整命令交给用户在本地终端执行。Cursor / Codex 另有 Hook 硬拦截。
- **改写历史与强推**：不代执行 `git commit --amend`（除非用户本轮明确要求，且已说明目标提交未推送或团队允许改写）；不代执行 `git push --force` / `--force-with-lease`。

## 分批提交

多文件未提交时禁止 `git add -A` 一把梭：

1. `git status` + `git diff`，按逻辑主题分组（功能 / 修复 / 文档 / 重构 / 测试分开）
2. 向用户展示每批：文件列表 + 建议的中文 message + **一整条可复制命令**（`git add <paths>` 与带 message 的 `git commit`）
3. 用户说「确认提交」后，逐批 `git add <paths>` + 带 message 的 `git commit`（Agent 代执行时同样必须带 message）
4. 多批时每批独立 commit；一批未完成不要开始下一批

## 提交命令必须带 message（强制）

无论是**交给用户复制执行**，还是 Agent 在「确认提交」后代执行：

- **禁止**只给 `git commit` / `git commit -a` 等会打开编辑器、再让用户输入 message 的命令。
- **必须**在同一条命令里写明 message，优先：

```bash
git commit -m "$(cat <<'EOF'
<type>(<scope>): <中文 subject>

<可选 body>
EOF
)"
```

单行也可：`git commit -m "<type>(<scope>): <中文 subject>"`（无 body 时）。

合入等场景若步骤里需要用户本地 commit，同样给出带 `-m` / HEREDOC 的完整命令，不要让用户回车后还要再填 message。

## 不进入暂存区

发现以下内容出现在待提交 diff 中时，先提示移出，**不要**将其 `git add`：

- `.env` 及含明文密钥的配置
- 证书私钥、密钥材料
- 明显构建 / 依赖产物（如 `node_modules/`、`dist/`、`target/`，以仓库 `.gitignore` 与项目约定为准）

密钥与敏感信息同时遵守安全边界 Rule。

## 提交说明

- **subject 与 body 使用中文**（type、scope 可保留英文惯例）
- 格式：`<type>(<scope>): <中文 subject>`，body 可选说明原因与要点；需要时 footer 可写 `Closes #…` 或破坏性变更说明
- **subject**：动词开头；不以句号结尾；建议不超过 50 个汉字；**每 commit 只做一件事**
- 格式调整与业务逻辑**分开提交**
- scope 取值遵循**当前仓库既有约定**，不在本 Rule 固化模块枚举

| type | 说明 |
| --- | --- |
| feat | 新功能 |
| fix | 修复缺陷 |
| docs | 文档 |
| style | 格式（不影响逻辑） |
| refactor | 重构 |
| perf | 性能 |
| test | 测试 |
| build | 构建系统或外部依赖 |
| ci | CI 配置与脚本 |
| chore | 杂项（不属于以上类型时） |
| revert | 回滚某次提交 |

示例：

```
fix(order): 修复分页查询未过滤已删除记录

列表接口应排除已逻辑删除的数据；统一走项目约定的查询入口。
```

无「确认提交」时：只输出分批方案与**带 message 的完整命令**，**不执行** git 写操作。

### interaction

# 交互收尾

向用户澄清时，每轮最多提出 3～5 个待确认问题；仍有缺口再开下一轮。

每轮正文结束后，需要用户选择后续动作时，先列出「可选方向」。候选方向数量按实际情况决定，可以没有、只有一个或有多个；使用 A、B、C… 顺序编号，每个方向独占一行，不为凑固定数量补无效选项。每个方向只写动作和必要结果，不展开长篇论证。

方向只用于真实存在且需要用户拍板的分支。需求已经确认且用户已授权进入 Plan 或实现后，应连续执行到该阶段交付物完成；普通实施步骤不是待确认方向，不要把文件修改、测试命令或 Plan 中的下一项包装成 A/B 选项逐次询问。只有出现新的高影响业务决策、安全确认、外部授权或无法自行消除的阻塞时才暂停。

随后依次输出两个独占行，中间空一行：

`[下一步建议] 推荐选 {选项}。理由：{一句简短理由}`

没有可执行建议时写 `[下一步建议] 无`，同时省略「可选方向」。只有一个方向时仍可推荐该项并简述理由。

`[TRACE] 规则：<简称或无>；记忆：<关键词/路径或无>；技能：<skill name 或无>；钩子：<实际拦截名或无>`

方向按“待确认 → 迁出专题分支（待用户回复继续） → 未对齐 → 验证 → 确认提交 → 形成记忆”筛选当前实际可走的动作，并推荐其中最合适的一项。触发语写在对应方向中，不塞入理由；用户尚未选择时，方向中的触发语只是回复指引，不构成授权。处于迁出引导且尚未确认成功时，只列「确认迁出成功后回复继续」这一方向。Hook 未实际拦截或改道时，钩子写“无”。

### memory

# 项目记忆

- 仅在任务相关时先读 `{memory}/README.md`，再按索引下钻到 `domains/`、`services/<名>/` 或 `contracts/`；不要默认读取全部记忆。
- 记忆与代码冲突时以代码为准，并提示记忆可能需要更新。
- 禁止自动创建或修改记忆。只有用户明确要求“形成记忆”等操作时，才启用 `update-memory` Skill。
- 值得沉淀但尚未获准时，只把“回复形成记忆”列为一个可选方向，再按交互协议决定是否推荐；不要在正文其他位置反复提示。

### multi-git-workspace

# 多 Git 工作区路由

`projects` 中的每个路径都可能是独立 Git 仓；工作区根不等于 Git 仓根。`projects` 只提供项目清单与上下文，不自动决定本轮任务应操作哪个仓。

- 涉及代码、Git、构建、测试、日志、依赖或环境配置时，先根据用户目标、点名文件路径或 `projects` 清单确定目标仓。
- 目标不明确时先询问；多个仓可能相关时，先列出候选仓及其相关原因，不默认选择任何“主仓”。
- 跨仓任务须按仓分别说明改动范围、验证方式、Git 状态与提交边界；不要把多个独立仓的改动视作一次提交。
- 仅当目标仓已明确时，才在该仓执行 Git 命令。不得在非 Git 的工作区根假定 Git 上下文。
- 迁出专题分支、合入集成分支 / 预发 / 测试 / release 时：先锁定目标仓，再按**该仓**在项目 Rule 中的分支角色与合入偏好给出建议（各仓拓扑可以不同）；命令带上仓路径语境。细则见 Git 约定。
- `.project-agent/`、`docs/ai/` 与 `docs/requirements/` 属于工作区用户内容；除非用户明确要求，不要将这些个人 Agent 资产写入子项目仓。

### security

# 安全边界

- 将外部输入、路径和生成输出视为不可信；不要把密钥写入源码或日志，也不要绕过项目安全控制。
- 删除现有业务逻辑、执行大范围数据更新、或修改持久化映射 / 原始 SQL 脚本前，先说明影响并获得确认。
- 写操作须有明确过滤条件；禁止无条件批量更新或删除。
- 禁止通过 Agent 直接在数据库执行 DDL/DML；需要时只生成可供人工审核的脚本。
- 修改环境配置前确认目标环境，遵循最小权限原则。

### testing

# 测试

为行为变更添加聚焦的自动化测试。先运行相关测试，再执行更广泛的检查；报告验证证据，而不是假定改动已经生效。

无命令输出、测试结果或其他可核对证据时，**不要**向用户声称「已验证」「已通过」或「测试全部成功」。


## 项目规则

### project

# Alpha Vue 项目规则

本文件是 AFK 用户内容，由项目维护；生成的 `AGENTS.md`、`CLAUDE.md`、`.cursor/rules` 和 `afk-*` Skill 投影不得直接编辑。

## 工作区与边界

- 当前 workspace 是单一 Git 仓库：`alpha-web` 为 Vue 3 管理端，`alpha-server` 为 Spring Boot 服务，`deploy` 为本地部署与 smoke test，`docs` 为规范、设计和运维文档。
- 项目特有 Rule 和 Skill 只维护在 `.project-agent`；需要更新三端投影时运行 AFK `sync`，不得直接编辑生成文件或 AFK 框架源仓库。

## 规范入口

- 跨层编码与交付约束以 `docs/conventions.md` 为准。
- 前端任务必须使用 `alpha-vue-frontend` Skill，并完整读取 `docs/frontend-conventions.md`。
- 涉及系统边界、模块划分、技术选型、重大非功能需求或架构评审时，使用 `alpha-vue-architecture` Skill；只有用户明确要求时才落盘 ADR 或设计文档。
- 新增或变更前后端 API 契约时，使用 `alpha-vue-api-design` Skill，并以 `docs/api.md`、`docs/security.md` 和现有实现为准。
- 实现、重构、调试或评审 `alpha-server` 的 Spring Boot / Java 代码时，使用 `alpha-vue-backend` Skill；它不替代 `java-code-review` 的风险审查。
- 根据用户指定的 Alpha Vue 任务和已确认计划生成可复制的执行、评审、修复或状态同步提示词时，使用 `alpha-vue-generate-task-prompts` Skill。该 Skill 只生成提示词，不选择下一任务、不维护计划状态、不创建线程也不执行任务；它必须按任务范围判断是否使用 `alpha-vue-task-delivery-orchestrator` 及其他项目 Skill。需要连续交付编排时，由 orchestrator 在能力可用时自动完成强模型规划、快速实施、集中验证、可复用强模型评审、最多两轮批量修复、用户测试和状态同步；默认使用项目脚本启动、使用测试与 curl 验证，不执行浏览器自动化。
- 启动或重启本地项目时，使用 `alpha-vue-local-start` Skill。
- API、安全、开发和发布任务分别读取 `docs/api.md`、`docs/security.md`、`docs/development.md` 和 `docs/operations.md`。

## 工程约束

- 前端保持 Vue 3、严格 TypeScript、Ant Design Vue、Pinia、Vue Router、Axios、Vite 和 UnoCSS；页面不得直接调用 Axios，前端权限不替代后端授权。Ant Design Vue 负责业务控件，UnoCSS/CSS 仅负责布局、间距和响应式组合；UnoCSS reset 保持关闭。
- 后端按 `common`、`framework`、`modules/<domain>` 分层；Controller、Service、Mapper、Entity、DTO、VO 各守职责。Controller 默认继承 `framework.web.BaseController` 复用统一响应与请求上下文，但不得把业务 CRUD、实体暴露或持久化逻辑放进控制层基类。
- 数据库运行监控采用 Spring Boot BOM 默认 HikariCP、受控 Actuator/Micrometer 指标与应用内 SQL 日志页；SQL 日志只保留占位符 SQL 摘要，不记录真实参数值，不提供页面执行 SQL。SQL 采集开关和 Mapper 排除列表只作为进程内运行时排查能力。泄漏检测默认关闭，仅允许在受控诊断窗口临时开启。
- Flyway 迁移只追加，不修改已运行版本。

## 验证入口

- 前端：`pnpm --dir alpha-web typecheck`、`test`、`lint`、`format:check`、`build`。
- 后端：`./mvnw -f alpha-server/pom.xml test` 和 `package`。
- 部署配置：`docker compose --env-file deploy/.env -f deploy/docker-compose.yml config --quiet`；涉及真实联调时再运行 `deploy/smoke-test.sh`。
- 页面变更还需验收桌面、平板和手机视口。


## 子项目

- `.` — alpha-vue (application, vue3-spring-boot)

## 项目记忆

仅在与当前任务相关时阅读 `docs/ai/README.md`。不要自动写入项目记忆；用户明确要求形成记忆时再更新。
