<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.onedream921.alphavue.modules.system.mapper.SysDictTypeMapper">

    <sql id="DictTypeColumns">
        id, type_code, type_name, status, remark, deleted, created_at, updated_at
    </sql>

    <select id="selectPageActive" resultType="io.github.onedream921.alphavue.modules.system.entity.SysDictType">
        SELECT <include refid="DictTypeColumns"/>
        FROM sys_dict_type
        WHERE deleted = 0
        ORDER BY id DESC
    </select>

    <select id="selectActiveById" resultType="io.github.onedream921.alphavue.modules.system.entity.SysDictType">
        SELECT <include refid="DictTypeColumns"/>
        FROM sys_dict_type
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectActiveByTypeCode" resultType="io.github.onedream921.alphavue.modules.system.entity.SysDictType">
        SELECT <include refid="DictTypeColumns"/>
        FROM sys_dict_type
        WHERE type_code = #{typeCode} AND deleted = 0
    </select>

    <select id="selectEnabledTypeCodes" resultType="string">
        SELECT type_code
        FROM sys_dict_type
        WHERE deleted = 0 AND status = 1
        ORDER BY type_code ASC
    </select>

    <insert id="insertType" useGeneratedKeys="true" keyProperty="type.id">
        INSERT INTO sys_dict_type (type_code, type_name, status, remark, deleted, created_at, updated_at)
        VALUES (#{type.typeCode}, #{type.typeName}, #{type.status}, #{type.remark}, 0,
                CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateType">
        UPDATE sys_dict_type
        SET type_name = #{type.typeName},
            status = #{type.status},
            remark = #{type.remark},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{type.id} AND deleted = 0
    </update>

    <update id="softDeleteById">
        UPDATE sys_dict_type
        SET deleted = id, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>
</mapper>
